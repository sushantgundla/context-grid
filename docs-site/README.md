# context-grid docs site

To preview this documentation locally:

1. Install the Mintlify CLI: `npm i -g mint`
2. From this directory (`docs-site`), run: `mint dev`
3. Open the URL it prints (usually `http://localhost:3000`)
